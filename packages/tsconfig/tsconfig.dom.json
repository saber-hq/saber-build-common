{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.module.json",
  "display": "Saber TSConfig for libraries that interact with the DOM (recommended)",
  "compilerOptions": {
    "lib": ["DOM", "ES2019"]
  }
}
